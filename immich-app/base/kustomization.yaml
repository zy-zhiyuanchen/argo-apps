apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storage/      # Deploy storage first
  - db/     # Then database (depends on storage)
  - app/          # Finally the app (depends on database)
